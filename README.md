# Infra Observability Lab

Docker Compose example with Nginx, Nginx exporter, and Prometheus. Run `docker compose up -d`; visit `localhost:8080` and `localhost:9090`. Run `sh scripts/send-traffic.sh` to generate sample requests.

Backdated sample commits in this repository are synthetic.
